[project]
name = "photos_compressor"
version = "0.2.0"
description = "this will compress the shit out of your photos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pillow>=11",
    "tqdm>=4.66",
]
